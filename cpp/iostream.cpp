#include <iostream>
using namespace std;

int main (int argc, char *argv[]){
  // cout is used for outputing date to terminal

  char str[] = "Pkay";
  cout << "name : " << str << endl;

  // cin is used for getting input from keyboard & storing it in a veriable

  int x,y;
  cin >> x >> y;
  cout << "x = " << x << "\t y = " << y << endl;

  return 0;
}
